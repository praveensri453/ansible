- name: install and run nginx
  hosts: web
  become: yes
  task:
  - name: install nginx
    ansible.builtin.package:
      name: nginx
      state: present
  - name: install nginx
    ansible.builtin.service:
      name: nginx
      state: started
      enabled:yes